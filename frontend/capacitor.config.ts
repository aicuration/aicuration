import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.honamuniv.aicuration',
  appName: 'aicurationApp',
  webDir: 'build'
};

export default config;
